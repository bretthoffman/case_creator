# Test fixture — mirrors business_rules/v1/case_creator_rules.yaml logical content.
# Regenerate when the seeded unified file changes.
# doctor_overrides is intentionally empty in the current business model (delivery is driven by
# delivery_modes.designer_doctor_names + shade_overrides.non_outsource_shades).

unified_version: 1
doctor_overrides:
  version: 1
  enabled: true
  rules: []
shade_overrides:
  version: 1
  enabled: true
  non_outsource_shades:
  - C3
  - A4
  rules: []
routing_overrides:
  version: 1
  enabled: true
  template_family_route_overrides:
  - family_key: argen
    destination_key: argen
  - family_key: study
    destination_key: '1_9'
  - family_key: anterior
    destination_key: '1_9'
  - family_key: ai
    destination_key: '1_9'
argen_modes:
  version: 1
  enabled: true
  contact_model_mode: 'off'
  contact_model_design_field: 3Shape Automate
