# Retired split YAML backup (read-only reference)

These four files are an **archival copy** of the last operator-facing split layout before **`business_rules/v1/case_creator_rules.yaml`** became the sole live source.

- The **app does not load** this folder.
- Edit **`business_rules/v1/case_creator_rules.yaml`** only for live behavior.
- Use this directory only for diffs, audits, or disaster recovery (manually merge back into the unified file if needed).

Files:

- `doctor_overrides.yaml`
- `shade_overrides.yaml`
- `routing_overrides.yaml`
- `argen_modes.yaml`

Archived as part of the unified split retirement pass (see `CASE_CREATOR_UNIFIED_RETIRE_SPLIT_REPORT.md`).
